{
  "name": "officeops-agent-infra-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "OfficeOps AgentTeams-compatible access incident demo with auditable mock tools and independent verification.",
  "scripts": {
    "dev": "node demo/server.mjs",
    "demo": "node demo/cli.mjs --mode normal",
    "demo:fake-success": "node demo/cli.mjs --mode fake_success",
    "test": "node --test demo/tests/*.test.mjs",
    "check": "node demo/scripts/check-config.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}

